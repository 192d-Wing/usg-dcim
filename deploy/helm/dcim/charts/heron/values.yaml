enabled: true
replicaCount: 3
resources:
  requests: { cpu: 500m, memory: 1Gi }
  limits:   { cpu: 2,    memory: 4Gi }
service:
  type: ClusterIP
  port: 8443
mtls:
  enabled: true
  caBundleSecret: dcim-collector-ca
