apiVersion: apps/v1
kind: Deployment
metadata:
  name: beagle
  namespace: dcim
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beagle
  template:
    metadata:
      labels:
        app: beagle
    spec:
      containers:
        - name: beagle
          image: localhost/dcim-beagle:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: DCIM_POSTGRES_DSN_RAW
              value: postgres://dcim:dcim@postgres:5432/dcim
            - name: DNS_PROBE_TICK
              value: 30s
            - name: DNS_PROBE_CONCURRENCY
              value: "64"
          securityContext:
            capabilities:
              add:
                - NET_RAW
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              memory: 1Gi
              cpu: 1000m
