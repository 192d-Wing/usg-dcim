# Top-level uv workspace. `uv sync` resolves all Python packages together.
# Animal map: otter = central API (was backend), mole = legacy collector (deprecated).
[tool.uv.workspace]
members = ["packages/otter", "packages/mole"]

[tool.uv]
# Pin a single Python interpreter across the workspace.
required-version = ">=0.5"
