// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: ipam.sql

package dbq

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countIPAddresses = `-- name: CountIPAddresses :one
SELECT count(*)::bigint
FROM ip_addresses
WHERE ($1::uuid IS NULL OR subnet_id = $1)
  AND ($2::uuid  IS NULL OR asset_id  = $2)
  AND ($3::text      IS NULL OR role::text   = $3)
  AND ($4::text    IS NULL OR status::text = $4)
  AND ($5::uuid[] IS NULL OR subnet_id IN (
        SELECT id FROM subnets WHERE fabric_id = ANY($5::uuid[])
      ))
`

type CountIPAddressesParams struct {
	SubnetID       *uuid.UUID  `json:"subnet_id"`
	AssetID        *uuid.UUID  `json:"asset_id"`
	Role           *string     `json:"role"`
	Status         *string     `json:"status"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

func (q *Queries) CountIPAddresses(ctx context.Context, arg CountIPAddressesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countIPAddresses,
		arg.SubnetID,
		arg.AssetID,
		arg.Role,
		arg.Status,
		arg.ScopeFabricIds,
	)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countSubnets = `-- name: CountSubnets :one
SELECT count(*)::bigint
FROM subnets
WHERE ($1::uuid IS NULL OR fabric_id = $1)
  AND ($2::uuid    IS NULL OR vrf_id    = $2)
  AND ($3::uuid   IS NULL OR site_id   = $3)
  AND ($4::text   IS NULL OR purpose   = $4)
  AND ($5::uuid[] IS NULL OR fabric_id = ANY($5::uuid[]))
`

type CountSubnetsParams struct {
	FabricID       *uuid.UUID  `json:"fabric_id"`
	VrfID          *uuid.UUID  `json:"vrf_id"`
	SiteID         *uuid.UUID  `json:"site_id"`
	Purpose        *string     `json:"purpose"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

func (q *Queries) CountSubnets(ctx context.Context, arg CountSubnetsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSubnets,
		arg.FabricID,
		arg.VrfID,
		arg.SiteID,
		arg.Purpose,
		arg.ScopeFabricIds,
	)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countVrfs = `-- name: CountVrfs :one
SELECT count(*)::bigint
FROM vrfs
WHERE ($1::uuid IS NULL OR fabric_id = $1)
  AND ($2::uuid[] IS NULL OR fabric_id = ANY($2::uuid[]))
`

type CountVrfsParams struct {
	FabricID       *uuid.UUID  `json:"fabric_id"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

func (q *Queries) CountVrfs(ctx context.Context, arg CountVrfsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countVrfs, arg.FabricID, arg.ScopeFabricIds)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const getIPAddress = `-- name: GetIPAddress :one
SELECT id, subnet_id, asset_id, host(address) AS address,
       role::text AS role, status::text AS status, source::text AS source,
       dns_name, description, dhcp_lease_expires_at, dhcp_mac,
       created_at, updated_at
FROM ip_addresses
WHERE id = $1
`

type GetIPAddressRow struct {
	ID                 uuid.UUID  `json:"id"`
	SubnetID           uuid.UUID  `json:"subnet_id"`
	AssetID            *uuid.UUID `json:"asset_id"`
	Address            string     `json:"address"`
	Role               string     `json:"role"`
	Status             string     `json:"status"`
	Source             string     `json:"source"`
	DnsName            *string    `json:"dns_name"`
	Description        *string    `json:"description"`
	DhcpLeaseExpiresAt *time.Time `json:"dhcp_lease_expires_at"`
	DhcpMac            *string    `json:"dhcp_mac"`
	CreatedAt          time.Time  `json:"created_at"`
	UpdatedAt          time.Time  `json:"updated_at"`
}

func (q *Queries) GetIPAddress(ctx context.Context, id uuid.UUID) (GetIPAddressRow, error) {
	row := q.db.QueryRow(ctx, getIPAddress, id)
	var i GetIPAddressRow
	err := row.Scan(
		&i.ID,
		&i.SubnetID,
		&i.AssetID,
		&i.Address,
		&i.Role,
		&i.Status,
		&i.Source,
		&i.DnsName,
		&i.Description,
		&i.DhcpLeaseExpiresAt,
		&i.DhcpMac,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSubnet = `-- name: GetSubnet :one
SELECT id, supernet_id, fabric_id, vrf_id, site_id,
       (host(prefix) || '/' || masklen(prefix))::text AS prefix,
       name, description, purpose, vlan_id,
       CASE WHEN gateway IS NULL THEN NULL ELSE host(gateway) END AS gateway, vni_id, created_at, updated_at
FROM subnets
WHERE id = $1
`

type GetSubnetRow struct {
	ID          uuid.UUID   `json:"id"`
	SupernetID  uuid.UUID   `json:"supernet_id"`
	FabricID    uuid.UUID   `json:"fabric_id"`
	VrfID       uuid.UUID   `json:"vrf_id"`
	SiteID      *uuid.UUID  `json:"site_id"`
	Prefix      string      `json:"prefix"`
	Name        *string     `json:"name"`
	Description *string     `json:"description"`
	Purpose     *string     `json:"purpose"`
	VlanID      *int32      `json:"vlan_id"`
	Gateway     interface{} `json:"gateway"`
	VniID       *uuid.UUID  `json:"vni_id"`
	CreatedAt   time.Time   `json:"created_at"`
	UpdatedAt   time.Time   `json:"updated_at"`
}

func (q *Queries) GetSubnet(ctx context.Context, id uuid.UUID) (GetSubnetRow, error) {
	row := q.db.QueryRow(ctx, getSubnet, id)
	var i GetSubnetRow
	err := row.Scan(
		&i.ID,
		&i.SupernetID,
		&i.FabricID,
		&i.VrfID,
		&i.SiteID,
		&i.Prefix,
		&i.Name,
		&i.Description,
		&i.Purpose,
		&i.VlanID,
		&i.Gateway,
		&i.VniID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getVrf = `-- name: GetVrf :one
SELECT id, fabric_id, name, description, is_default, created_at, updated_at, route_target
FROM vrfs
WHERE id = $1
`

func (q *Queries) GetVrf(ctx context.Context, id uuid.UUID) (Vrf, error) {
	row := q.db.QueryRow(ctx, getVrf, id)
	var i Vrf
	err := row.Scan(
		&i.ID,
		&i.FabricID,
		&i.Name,
		&i.Description,
		&i.IsDefault,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RouteTarget,
	)
	return i, err
}

const listAddressStringsInSubnet = `-- name: ListAddressStringsInSubnet :many
SELECT host(address) AS address
FROM ip_addresses
WHERE subnet_id = $1
`

// PR 64 — subnet utilization reads only the address column. Projecting
// the single column keeps the read fast on busy subnets and matches
// the Python services.ipam.used_addresses_in_subnet shape: one row
// per allocated address, stripped of the /N mask.
func (q *Queries) ListAddressStringsInSubnet(ctx context.Context, subnetID uuid.UUID) ([]string, error) {
	rows, err := q.db.Query(ctx, listAddressStringsInSubnet, subnetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var address string
		if err := rows.Scan(&address); err != nil {
			return nil, err
		}
		items = append(items, address)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAddressesInSubnets = `-- name: ListAddressesInSubnets :many
SELECT subnet_id, host(address) AS address
FROM ip_addresses
WHERE subnet_id = ANY($1::uuid[])
`

type ListAddressesInSubnetsRow struct {
	SubnetID uuid.UUID `json:"subnet_id"`
	Address  string    `json:"address"`
}

// PR 65 — bulk per-subnet address counting. One round-trip instead
// of N+1; the handler buckets by subnet_id in-process. Used by
// free-space/in-subnets to compute allocated + next_available for
// many subnets in a single query.
func (q *Queries) ListAddressesInSubnets(ctx context.Context, subnetIds []uuid.UUID) ([]ListAddressesInSubnetsRow, error) {
	rows, err := q.db.Query(ctx, listAddressesInSubnets, subnetIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListAddressesInSubnetsRow
	for rows.Next() {
		var i ListAddressesInSubnetsRow
		if err := rows.Scan(&i.SubnetID, &i.Address); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listIPAddresses = `-- name: ListIPAddresses :many
SELECT id, subnet_id, asset_id, host(address) AS address,
       role::text AS role, status::text AS status, source::text AS source,
       dns_name, description, dhcp_lease_expires_at, dhcp_mac,
       created_at, updated_at
FROM ip_addresses
WHERE ($3::uuid IS NULL OR subnet_id = $3)
  AND ($4::uuid  IS NULL OR asset_id  = $4)
  AND ($5::text      IS NULL OR role::text   = $5)
  AND ($6::text    IS NULL OR status::text = $6)
  AND ($7::uuid[] IS NULL OR subnet_id IN (
        SELECT id FROM subnets WHERE fabric_id = ANY($7::uuid[])
      ))
ORDER BY address
LIMIT $1 OFFSET $2
`

type ListIPAddressesParams struct {
	Limit          int32       `json:"limit"`
	Offset         int32       `json:"offset"`
	SubnetID       *uuid.UUID  `json:"subnet_id"`
	AssetID        *uuid.UUID  `json:"asset_id"`
	Role           *string     `json:"role"`
	Status         *string     `json:"status"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

type ListIPAddressesRow struct {
	ID                 uuid.UUID  `json:"id"`
	SubnetID           uuid.UUID  `json:"subnet_id"`
	AssetID            *uuid.UUID `json:"asset_id"`
	Address            string     `json:"address"`
	Role               string     `json:"role"`
	Status             string     `json:"status"`
	Source             string     `json:"source"`
	DnsName            *string    `json:"dns_name"`
	Description        *string    `json:"description"`
	DhcpLeaseExpiresAt *time.Time `json:"dhcp_lease_expires_at"`
	DhcpMac            *string    `json:"dhcp_mac"`
	CreatedAt          time.Time  `json:"created_at"`
	UpdatedAt          time.Time  `json:"updated_at"`
}

// ===== IP Addresses =====
// 2-hop scope: address → subnet → fabric. Subnets denormalize fabric_id
// so the subquery is a single index scan.
func (q *Queries) ListIPAddresses(ctx context.Context, arg ListIPAddressesParams) ([]ListIPAddressesRow, error) {
	rows, err := q.db.Query(ctx, listIPAddresses,
		arg.Limit,
		arg.Offset,
		arg.SubnetID,
		arg.AssetID,
		arg.Role,
		arg.Status,
		arg.ScopeFabricIds,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListIPAddressesRow
	for rows.Next() {
		var i ListIPAddressesRow
		if err := rows.Scan(
			&i.ID,
			&i.SubnetID,
			&i.AssetID,
			&i.Address,
			&i.Role,
			&i.Status,
			&i.Source,
			&i.DnsName,
			&i.Description,
			&i.DhcpLeaseExpiresAt,
			&i.DhcpMac,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSubnets = `-- name: ListSubnets :many
SELECT id, supernet_id, fabric_id, vrf_id, site_id,
       (host(prefix) || '/' || masklen(prefix))::text AS prefix,
       name, description, purpose, vlan_id,
       CASE WHEN gateway IS NULL THEN NULL ELSE host(gateway) END AS gateway, vni_id, created_at, updated_at
FROM subnets
WHERE ($3::uuid IS NULL OR fabric_id = $3)
  AND ($4::uuid    IS NULL OR vrf_id    = $4)
  AND ($5::uuid   IS NULL OR site_id   = $5)
  AND ($6::text   IS NULL OR purpose   = $6)
  AND ($7::uuid[] IS NULL OR fabric_id = ANY($7::uuid[]))
ORDER BY prefix
LIMIT $1 OFFSET $2
`

type ListSubnetsParams struct {
	Limit          int32       `json:"limit"`
	Offset         int32       `json:"offset"`
	FabricID       *uuid.UUID  `json:"fabric_id"`
	VrfID          *uuid.UUID  `json:"vrf_id"`
	SiteID         *uuid.UUID  `json:"site_id"`
	Purpose        *string     `json:"purpose"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

type ListSubnetsRow struct {
	ID          uuid.UUID   `json:"id"`
	SupernetID  uuid.UUID   `json:"supernet_id"`
	FabricID    uuid.UUID   `json:"fabric_id"`
	VrfID       uuid.UUID   `json:"vrf_id"`
	SiteID      *uuid.UUID  `json:"site_id"`
	Prefix      string      `json:"prefix"`
	Name        *string     `json:"name"`
	Description *string     `json:"description"`
	Purpose     *string     `json:"purpose"`
	VlanID      *int32      `json:"vlan_id"`
	Gateway     interface{} `json:"gateway"`
	VniID       *uuid.UUID  `json:"vni_id"`
	CreatedAt   time.Time   `json:"created_at"`
	UpdatedAt   time.Time   `json:"updated_at"`
}

// ===== Subnets =====
func (q *Queries) ListSubnets(ctx context.Context, arg ListSubnetsParams) ([]ListSubnetsRow, error) {
	rows, err := q.db.Query(ctx, listSubnets,
		arg.Limit,
		arg.Offset,
		arg.FabricID,
		arg.VrfID,
		arg.SiteID,
		arg.Purpose,
		arg.ScopeFabricIds,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSubnetsRow
	for rows.Next() {
		var i ListSubnetsRow
		if err := rows.Scan(
			&i.ID,
			&i.SupernetID,
			&i.FabricID,
			&i.VrfID,
			&i.SiteID,
			&i.Prefix,
			&i.Name,
			&i.Description,
			&i.Purpose,
			&i.VlanID,
			&i.Gateway,
			&i.VniID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSubnetsForFreeSpace = `-- name: ListSubnetsForFreeSpace :many
SELECT id, fabric_id, vrf_id, site_id,
       (host(prefix) || '/' || masklen(prefix))::text AS prefix,
       name, purpose
FROM subnets
WHERE ($1::uuid IS NULL OR fabric_id = $1)
  AND ($2::uuid    IS NULL OR vrf_id    = $2)
ORDER BY prefix
`

type ListSubnetsForFreeSpaceParams struct {
	FabricID *uuid.UUID `json:"fabric_id"`
	VrfID    *uuid.UUID `json:"vrf_id"`
}

type ListSubnetsForFreeSpaceRow struct {
	ID       uuid.UUID  `json:"id"`
	FabricID uuid.UUID  `json:"fabric_id"`
	VrfID    uuid.UUID  `json:"vrf_id"`
	SiteID   *uuid.UUID `json:"site_id"`
	Prefix   string     `json:"prefix"`
	Name     *string    `json:"name"`
	Purpose  *string    `json:"purpose"`
}

// PR 65 — free-space/in-subnets reads every matching subnet
// (unpaginated) and filters in-process by family + free count.
// Matches the Python handler's db.execute(stmt).scalars().all()
// — no LIMIT. Returns just the columns the response shape needs.
func (q *Queries) ListSubnetsForFreeSpace(ctx context.Context, arg ListSubnetsForFreeSpaceParams) ([]ListSubnetsForFreeSpaceRow, error) {
	rows, err := q.db.Query(ctx, listSubnetsForFreeSpace, arg.FabricID, arg.VrfID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSubnetsForFreeSpaceRow
	for rows.Next() {
		var i ListSubnetsForFreeSpaceRow
		if err := rows.Scan(
			&i.ID,
			&i.FabricID,
			&i.VrfID,
			&i.SiteID,
			&i.Prefix,
			&i.Name,
			&i.Purpose,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVrfs = `-- name: ListVrfs :many
SELECT id, fabric_id, name, description, is_default, created_at, updated_at, route_target
FROM vrfs
WHERE ($3::uuid IS NULL OR fabric_id = $3)
  AND ($4::uuid[] IS NULL OR fabric_id = ANY($4::uuid[]))
ORDER BY name
LIMIT $1 OFFSET $2
`

type ListVrfsParams struct {
	Limit          int32       `json:"limit"`
	Offset         int32       `json:"offset"`
	FabricID       *uuid.UUID  `json:"fabric_id"`
	ScopeFabricIds []uuid.UUID `json:"scope_fabric_ids"`
}

// ===== VRFs =====
// scope_fabric_ids is the PR 56 ABAC LIST filter: NULL = no restriction
// (global principal); a non-NULL slice restricts the result to vrfs in
// the named fabrics. See auth.ScopedFabricFilter.
func (q *Queries) ListVrfs(ctx context.Context, arg ListVrfsParams) ([]Vrf, error) {
	rows, err := q.db.Query(ctx, listVrfs,
		arg.Limit,
		arg.Offset,
		arg.FabricID,
		arg.ScopeFabricIds,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Vrf
	for rows.Next() {
		var i Vrf
		if err := rows.Scan(
			&i.ID,
			&i.FabricID,
			&i.Name,
			&i.Description,
			&i.IsDefault,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.RouteTarget,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
