// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: bgp_tcp_ao.sql

package dbq

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countKeysInTcpAoKeyChain = `-- name: CountKeysInTcpAoKeyChain :one
SELECT count(*)::bigint
FROM tcp_ao_keys
WHERE key_chain_id = $1
`

// DELETE guard: Python refuses to drop a chain that still has keys
// so rotation history stays intentional. Handler reads this before
// DeleteTcpAoKeyChain and 409s if it's non-zero.
func (q *Queries) CountKeysInTcpAoKeyChain(ctx context.Context, keyChainID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countKeysInTcpAoKeyChain, keyChainID)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countTcpAoKeyChains = `-- name: CountTcpAoKeyChains :one
SELECT count(*)::bigint FROM tcp_ao_key_chains
`

func (q *Queries) CountTcpAoKeyChains(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countTcpAoKeyChains)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countTcpAoKeys = `-- name: CountTcpAoKeys :one
SELECT count(*)::bigint
FROM tcp_ao_keys
WHERE ($1::uuid IS NULL OR key_chain_id = $1)
`

func (q *Queries) CountTcpAoKeys(ctx context.Context, keyChainID *uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countTcpAoKeys, keyChainID)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const createTcpAoKey = `-- name: CreateTcpAoKey :one
INSERT INTO tcp_ao_keys (id, key_chain_id, key_id, send_id, recv_id,
                         algorithm, secret, valid_from, valid_to,
                         description, created_at, updated_at)
VALUES (gen_random_uuid(), $1, $2, $3, $4,
        $5::tcp_ao_algorithm, $6, $7, $8,
        $9, NOW(), NOW())
RETURNING id, key_chain_id, key_id, send_id, recv_id, algorithm, secret, description, created_at, updated_at, valid_from, valid_to
`

type CreateTcpAoKeyParams struct {
	KeyChainID  uuid.UUID  `json:"key_chain_id"`
	KeyID       int32      `json:"key_id"`
	SendID      int32      `json:"send_id"`
	RecvID      int32      `json:"recv_id"`
	Algorithm   string     `json:"algorithm"`
	Secret      string     `json:"secret"`
	ValidFrom   *time.Time `json:"valid_from"`
	ValidTo     *time.Time `json:"valid_to"`
	Description *string    `json:"description"`
}

func (q *Queries) CreateTcpAoKey(ctx context.Context, arg CreateTcpAoKeyParams) (TcpAoKey, error) {
	row := q.db.QueryRow(ctx, createTcpAoKey,
		arg.KeyChainID,
		arg.KeyID,
		arg.SendID,
		arg.RecvID,
		arg.Algorithm,
		arg.Secret,
		arg.ValidFrom,
		arg.ValidTo,
		arg.Description,
	)
	var i TcpAoKey
	err := row.Scan(
		&i.ID,
		&i.KeyChainID,
		&i.KeyID,
		&i.SendID,
		&i.RecvID,
		&i.Algorithm,
		&i.Secret,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ValidFrom,
		&i.ValidTo,
	)
	return i, err
}

const createTcpAoKeyChain = `-- name: CreateTcpAoKeyChain :one
INSERT INTO tcp_ao_key_chains (id, name, description, created_at, updated_at)
VALUES (gen_random_uuid(), $1, $2, NOW(), NOW())
RETURNING id, name, description, created_at, updated_at
`

type CreateTcpAoKeyChainParams struct {
	Name        string  `json:"name"`
	Description *string `json:"description"`
}

func (q *Queries) CreateTcpAoKeyChain(ctx context.Context, arg CreateTcpAoKeyChainParams) (TcpAoKeyChain, error) {
	row := q.db.QueryRow(ctx, createTcpAoKeyChain, arg.Name, arg.Description)
	var i TcpAoKeyChain
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTcpAoKey = `-- name: DeleteTcpAoKey :exec
DELETE FROM tcp_ao_keys WHERE id = $1
`

func (q *Queries) DeleteTcpAoKey(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteTcpAoKey, id)
	return err
}

const deleteTcpAoKeyChain = `-- name: DeleteTcpAoKeyChain :exec
DELETE FROM tcp_ao_key_chains WHERE id = $1
`

func (q *Queries) DeleteTcpAoKeyChain(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteTcpAoKeyChain, id)
	return err
}

const getTcpAoKey = `-- name: GetTcpAoKey :one
SELECT id, key_chain_id, key_id, send_id, recv_id, algorithm, secret, description, created_at, updated_at, valid_from, valid_to
FROM tcp_ao_keys
WHERE id = $1
`

func (q *Queries) GetTcpAoKey(ctx context.Context, id uuid.UUID) (TcpAoKey, error) {
	row := q.db.QueryRow(ctx, getTcpAoKey, id)
	var i TcpAoKey
	err := row.Scan(
		&i.ID,
		&i.KeyChainID,
		&i.KeyID,
		&i.SendID,
		&i.RecvID,
		&i.Algorithm,
		&i.Secret,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ValidFrom,
		&i.ValidTo,
	)
	return i, err
}

const getTcpAoKeyChain = `-- name: GetTcpAoKeyChain :one
SELECT id, name, description, created_at, updated_at
FROM tcp_ao_key_chains
WHERE id = $1
`

func (q *Queries) GetTcpAoKeyChain(ctx context.Context, id uuid.UUID) (TcpAoKeyChain, error) {
	row := q.db.QueryRow(ctx, getTcpAoKeyChain, id)
	var i TcpAoKeyChain
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTcpAoKeyChains = `-- name: ListTcpAoKeyChains :many

SELECT id, name, description, created_at, updated_at
FROM tcp_ao_key_chains
ORDER BY name
LIMIT $1 OFFSET $2
`

type ListTcpAoKeyChainsParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

// TCP AO key-chains CRUD. Keys + rotate-batch land in a follow-up PR.
func (q *Queries) ListTcpAoKeyChains(ctx context.Context, arg ListTcpAoKeyChainsParams) ([]TcpAoKeyChain, error) {
	rows, err := q.db.Query(ctx, listTcpAoKeyChains, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TcpAoKeyChain
	for rows.Next() {
		var i TcpAoKeyChain
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTcpAoKeys = `-- name: ListTcpAoKeys :many

SELECT id, key_chain_id, key_id, send_id, recv_id, algorithm, secret, description, created_at, updated_at, valid_from, valid_to
FROM tcp_ao_keys
WHERE ($3::uuid IS NULL OR key_chain_id = $3)
ORDER BY key_chain_id, key_id
LIMIT $1 OFFSET $2
`

type ListTcpAoKeysParams struct {
	Limit      int32      `json:"limit"`
	Offset     int32      `json:"offset"`
	KeyChainID *uuid.UUID `json:"key_chain_id"`
}

// ----- Keys -----
// Optional key_chain_id filter narrows to one chain's history.
func (q *Queries) ListTcpAoKeys(ctx context.Context, arg ListTcpAoKeysParams) ([]TcpAoKey, error) {
	rows, err := q.db.Query(ctx, listTcpAoKeys, arg.Limit, arg.Offset, arg.KeyChainID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TcpAoKey
	for rows.Next() {
		var i TcpAoKey
		if err := rows.Scan(
			&i.ID,
			&i.KeyChainID,
			&i.KeyID,
			&i.SendID,
			&i.RecvID,
			&i.Algorithm,
			&i.Secret,
			&i.Description,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.ValidFrom,
			&i.ValidTo,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const maxKeyIDInTcpAoKeyChain = `-- name: MaxKeyIDInTcpAoKeyChain :one
SELECT COALESCE(MAX(key_id), 0)::int AS max_key_id
FROM tcp_ao_keys
WHERE key_chain_id = $1
`

// Used by the rotate-batch handler to pick the next free key_id so a
// second rotation extends the chain rather than colliding. Returns 0
// when the chain has no keys (COALESCE keeps the type clean for the
// handler's Go-side increment).
func (q *Queries) MaxKeyIDInTcpAoKeyChain(ctx context.Context, keyChainID uuid.UUID) (int32, error) {
	row := q.db.QueryRow(ctx, maxKeyIDInTcpAoKeyChain, keyChainID)
	var max_key_id int32
	err := row.Scan(&max_key_id)
	return max_key_id, err
}

const updateTcpAoKey = `-- name: UpdateTcpAoKey :one
UPDATE tcp_ao_keys
SET key_id      = COALESCE($2::int, key_id),
    send_id     = COALESCE($3::int, send_id),
    recv_id     = COALESCE($4::int, recv_id),
    algorithm   = COALESCE($5::tcp_ao_algorithm, algorithm),
    secret      = COALESCE($6::text, secret),
    valid_from  = CASE WHEN $7::bool THEN $8::timestamptz ELSE valid_from END,
    valid_to    = CASE WHEN $9::bool   THEN $10::timestamptz   ELSE valid_to   END,
    description = CASE WHEN $11::bool THEN $12::text ELSE description END,
    updated_at  = NOW()
WHERE id = $1
RETURNING id, key_chain_id, key_id, send_id, recv_id, algorithm, secret, description, created_at, updated_at, valid_from, valid_to
`

type UpdateTcpAoKeyParams struct {
	ID             uuid.UUID  `json:"id"`
	KeyID          *int32     `json:"key_id"`
	SendID         *int32     `json:"send_id"`
	RecvID         *int32     `json:"recv_id"`
	Algorithm      *string    `json:"algorithm"`
	Secret         *string    `json:"secret"`
	ValidFromSet   bool       `json:"valid_from_set"`
	ValidFrom      *time.Time `json:"valid_from"`
	ValidToSet     bool       `json:"valid_to_set"`
	ValidTo        *time.Time `json:"valid_to"`
	DescriptionSet bool       `json:"description_set"`
	Description    *string    `json:"description"`
}

// key_chain_id is intentionally not patchable — moving a key across
// chains would orphan rotation history and break the (chain_id,
// key_id) unique index downstream. Operators delete + recreate to
// relocate.
func (q *Queries) UpdateTcpAoKey(ctx context.Context, arg UpdateTcpAoKeyParams) (TcpAoKey, error) {
	row := q.db.QueryRow(ctx, updateTcpAoKey,
		arg.ID,
		arg.KeyID,
		arg.SendID,
		arg.RecvID,
		arg.Algorithm,
		arg.Secret,
		arg.ValidFromSet,
		arg.ValidFrom,
		arg.ValidToSet,
		arg.ValidTo,
		arg.DescriptionSet,
		arg.Description,
	)
	var i TcpAoKey
	err := row.Scan(
		&i.ID,
		&i.KeyChainID,
		&i.KeyID,
		&i.SendID,
		&i.RecvID,
		&i.Algorithm,
		&i.Secret,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ValidFrom,
		&i.ValidTo,
	)
	return i, err
}

const updateTcpAoKeyChain = `-- name: UpdateTcpAoKeyChain :one
UPDATE tcp_ao_key_chains
SET name        = COALESCE($2::text, name),
    description = CASE WHEN $3::bool THEN $4::text ELSE description END,
    updated_at  = NOW()
WHERE id = $1
RETURNING id, name, description, created_at, updated_at
`

type UpdateTcpAoKeyChainParams struct {
	ID             uuid.UUID `json:"id"`
	Name           *string   `json:"name"`
	DescriptionSet bool      `json:"description_set"`
	Description    *string   `json:"description"`
}

// name is NOT NULL; description is nullable. Both use the COALESCE
// semantic (omit = keep current) plus an explicit set-flag for
// description so `{"description": null}` clears the row instead of
// silently no-op'ing. Matches Pydantic exclude_unset=True parity.
func (q *Queries) UpdateTcpAoKeyChain(ctx context.Context, arg UpdateTcpAoKeyChainParams) (TcpAoKeyChain, error) {
	row := q.db.QueryRow(ctx, updateTcpAoKeyChain,
		arg.ID,
		arg.Name,
		arg.DescriptionSet,
		arg.Description,
	)
	var i TcpAoKeyChain
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
