// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: dhcp_reconcile.sql

package dbq

import (
	"context"

	"github.com/google/uuid"
)

const insertReservationIPAddress = `-- name: InsertReservationIPAddress :one

INSERT INTO ip_addresses (
    id, subnet_id, address, role, status, source,
    dhcp_mac, dhcp_duid, dns_name,
    created_at, updated_at
)
VALUES (gen_random_uuid(), $1, $2::inet,
        'data', 'reserved', 'reservation',
        $3, $4, $5,
        NOW(), NOW())
RETURNING id
`

type InsertReservationIPAddressParams struct {
	SubnetID uuid.UUID `json:"subnet_id"`
	Address  string    `json:"address"`
	DhcpMac  *string   `json:"dhcp_mac"`
	DhcpDuid *string   `json:"dhcp_duid"`
	DnsName  *string   `json:"dns_name"`
}

// ===== Mutating reconcile sync (PR 13) =====
// Ports services/dhcp_reconcile.py:sync_reservations. INSERT for the
// "unbacked" branch (creates a fresh reservation row); UPDATE for
// the "promote dhcp → reservation" branch (flips an existing lease
// to reservation, backfills mac/duid/dns_name when they're NULL on
// the row but set on the reservation).
// Creates a fresh IPAddress with source=reservation, status=reserved.
// role defaults to 'data' per Python at services/dhcp_reconcile.py:306.
// Returns id so the handler can reference it in the per-entry
// decision payload.
func (q *Queries) InsertReservationIPAddress(ctx context.Context, arg InsertReservationIPAddressParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, insertReservationIPAddress,
		arg.SubnetID,
		arg.Address,
		arg.DhcpMac,
		arg.DhcpDuid,
		arg.DnsName,
	)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}

const listIPAddressesInSubnetForReconcile = `-- name: ListIPAddressesInSubnetForReconcile :many

SELECT id, address::text AS address, source::text AS source,
       dhcp_mac, dhcp_duid
FROM ip_addresses
WHERE subnet_id = $1
`

type ListIPAddressesInSubnetForReconcileRow struct {
	ID       uuid.UUID `json:"id"`
	Address  string    `json:"address"`
	Source   string    `json:"source"`
	DhcpMac  *string   `json:"dhcp_mac"`
	DhcpDuid *string   `json:"dhcp_duid"`
}

// DHCP reservation ↔ IPAM reconciliation (PR 12 of the DHCP push
// port). Ports Python's services/dhcp_reconcile.py's reconcile_scope.
// The orchestrator loads every IPAddress row in the scope's linked
// subnet, cross-checks each reservation in scope.reservations_json,
// and emits a per-reservation report.
//
// Narrow projection: only the four columns the matcher reads. The
// standard IPAddress projection omits dhcp_duid (PR 94 in Python);
// the reconcile path needs both dhcp_mac (v4 binding check, PR 88)
// and dhcp_duid (v6 binding check, PR 94), so we materialize them
// here instead of widening the global IPAddress projection.
func (q *Queries) ListIPAddressesInSubnetForReconcile(ctx context.Context, subnetID uuid.UUID) ([]ListIPAddressesInSubnetForReconcileRow, error) {
	rows, err := q.db.Query(ctx, listIPAddressesInSubnetForReconcile, subnetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListIPAddressesInSubnetForReconcileRow
	for rows.Next() {
		var i ListIPAddressesInSubnetForReconcileRow
		if err := rows.Scan(
			&i.ID,
			&i.Address,
			&i.Source,
			&i.DhcpMac,
			&i.DhcpDuid,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const promoteDhcpLeaseToReservation = `-- name: PromoteDhcpLeaseToReservation :exec
UPDATE ip_addresses
SET source     = 'reservation',
    status     = 'reserved',
    dhcp_mac   = COALESCE(dhcp_mac,  $1::text),
    dhcp_duid  = COALESCE(dhcp_duid, $2::text),
    dns_name   = COALESCE(dns_name,  $3::text),
    updated_at = NOW()
WHERE id = $4
`

type PromoteDhcpLeaseToReservationParams struct {
	DhcpMac  *string   `json:"dhcp_mac"`
	DhcpDuid *string   `json:"dhcp_duid"`
	DnsName  *string   `json:"dns_name"`
	ID       uuid.UUID `json:"id"`
}

// Flips an existing source='dhcp' row to source='reservation' +
// status='reserved'. Backfills dhcp_mac / dhcp_duid / dns_name when
// the reservation knows them but the row's column is NULL — never
// overwrites a populated column (Python at services/dhcp_reconcile.
// py:354-367). The COALESCE-on-NULL guards encode the "only
// backfill, don't overwrite" rule entirely on the DB side.
func (q *Queries) PromoteDhcpLeaseToReservation(ctx context.Context, arg PromoteDhcpLeaseToReservationParams) error {
	_, err := q.db.Exec(ctx, promoteDhcpLeaseToReservation,
		arg.DhcpMac,
		arg.DhcpDuid,
		arg.DnsName,
		arg.ID,
	)
	return err
}
