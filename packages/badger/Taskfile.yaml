version: '3'

# Tasks for badger. Invoke as 'task badger:<name>' from repo root.

tasks:
  build:
    desc: go build
    cmds: [go build ./...]
  vet:
    desc: go vet
    cmds: [go vet ./...]
  test:
    desc: go test
    cmds: [go test -count=1 ./...]
