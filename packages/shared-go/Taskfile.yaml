version: '3'

# Tasks for shared-go. Invoke as 'task shared-go:<name>' from repo root.

tasks:
  build:
    desc: go build
    cmds: [go build ./...]
  vet:
    desc: go vet
    cmds: [go vet ./...]
  test:
    desc: go test
    cmds: [go test -count=1 ./...]
